name: Fix require-run-as-non-root-user

on:
  workflow_dispatch:

jobs:
  fix:
    uses: nirmata/pipeline-scanning/.github/workflows/install-remediator.yml@helm-app
    with:
      require_run_as_non_root_user: true
    secrets: inherit
