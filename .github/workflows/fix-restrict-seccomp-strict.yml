name: Fix restrict-seccomp-strict

on:
  workflow_dispatch:

jobs:
  fix:
    uses: nirmata/pipeline-scanning/.github/workflows/install-remediator.yml@helm-app
    with:
      restrict_seccomp_strict: true
    secrets: inherit
