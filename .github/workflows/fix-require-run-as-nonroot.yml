name: Fix require-run-as-nonroot

on:
  workflow_dispatch:

jobs:
  fix:
    uses: nirmata/pipeline-scanning/.github/workflows/install-remediator.yml@helm-app
    with:
      require_run_as_nonroot: true
    secrets: inherit
